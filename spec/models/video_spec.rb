require "rails_helper"

describe Video do
  it "can save a video with the proper metadata"
  it "won't accept a non MP4 file"
  it "won't accept a video without a title"
  it "won't accept a video that's over 5 megabytes"
end
